import type { ColorMode,ThemeStyle } from '../types'
interface Props{themeStyle:ThemeStyle;colorMode:ColorMode;onThemeStyle:(value:ThemeStyle)=>void;onColorMode:(value:ColorMode)=>void}
export function ThemeControls({themeStyle,colorMode,onThemeStyle,onColorMode}:Props){return <div className="theme-controls" aria-label="Настройки оформления"><div className="segmented" aria-label="Стиль интерфейса"><button className={themeStyle==='modern'?'active':''} onClick={()=>onThemeStyle('modern')}>Modern</button><button className={themeStyle==='cozy'?'active':''} onClick={()=>onThemeStyle('cozy')}>Cozy</button></div><div className="segmented mode" aria-label="Цветовая схема">{(['light','system','dark'] as ColorMode[]).map(mode=><button key={mode} className={colorMode===mode?'active':''} onClick={()=>onColorMode(mode)} aria-label={mode}>{mode==='light'?'☀':mode==='dark'?'☾':'◐'}</button>)}</div></div>}
