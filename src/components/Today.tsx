import { todayPlan } from '../data/seed'
import type { LearnerState } from '../types'
interface Props{learner:LearnerState;dueCount:number;onStart:()=>void;onOpenModule:(module:string)=>void}
export function Today({learner,dueCount,onStart,onOpenModule}:Props){return <div className="page today-page"><section className="welcome-row"><div><p className="eyebrow">THURSDAY · YOUR ENGLISH</p><h1>Привет, {learner.name}! <span className="sun">✦</span></h1><p className="muted">Сегодня достаточно одного хорошего занятия.</p></div><div className="streak-pill"><span>🔥</span><strong>{learner.streak}</strong><small>дней</small></div></section><section className="hero-card"><div className="hero-copy"><span className="hero-kicker">ТВОЙ ПЛАН НА СЕГОДНЯ</span><h2>Продолжить обучение</h2><p>{dueCount>0?`${dueCount} элементов готовы к повторению`:'Повторения закрыты — можно изучить новое'}</p><div className="progress-track"><span style={{width:'38%'}} /></div><button className="primary-button" onClick={onStart}>Начать сессию <span>→</span></button></div><div className="hero-visual" aria-hidden="true"><div className="landscape"><span className="moon"/><span className="mountain mountain-a"/><span className="mountain mountain-b"/><span className="path"/></div><blockquote>Small steps.<br/>Real fluency.</blockquote></div></section><section className="section-block"><div className="section-heading"><div><p className="eyebrow">BALANCED PRACTICE</p><h2>Сегодняшний план</h2></div><button className="text-button">Настроить</button></div><div className="plan-grid">{todayPlan.map(item=><button key={item.id} className={`plan-card tone-${item.tone}`} onClick={()=>onOpenModule(item.id)}><span className="plan-icon">{item.icon}</span><strong>{item.title}</strong><small>{item.id==='review'&&dueCount>0?`${dueCount} сейчас`:item.subtitle}</small></button>)}</div></section><section className="section-block stats-block"><div className="section-heading"><div><p className="eyebrow">WHAT IS ACTUALLY STICKING</p><h2>Твой язык</h2></div><span className="level-chip">{learner.level}</span></div><div className="stats-grid"><Stat number={learner.totalVocabulary.toLocaleString('ru-RU')} label="Встречал"/><Stat number={learner.activeVocabulary.toLocaleString('ru-RU')} label="Активный словарь"/><Stat number={`${learner.irregularKnown}/${learner.irregularTotal}`} label="Глаголы"/><Stat number={learner.expressions.toLocaleString('ru-RU')} label="Выражения"/></div></section></div>}
function Stat({number,label}:{number:string;label:string}){return <div className="stat-card"><strong>{number}</strong><span>{label}</span></div>}
