import type { ColorMode,LearnerState,ThemeStyle } from '../types'
import { ThemeControls } from './ThemeControls'
interface Props{learner:LearnerState;onThemeStyle:(value:ThemeStyle)=>void;onColorMode:(value:ColorMode)=>void;onReset:()=>void}
export function Profile({learner,onThemeStyle,onColorMode,onReset}:Props){return <div className="page generic-page"><p className="eyebrow">YOUR LEARNING SYSTEM</p><h1>Профиль</h1><div className="profile-card"><div className="avatar">A</div><div><h2>{learner.name}</h2><p>{learner.level} · {learner.streak} дней практики подряд</p></div></div><section className="settings-card"><div><p className="eyebrow">APPEARANCE</p><h2>Оформление</h2><p>Обе утверждённые дизайн-системы равноправны и поддерживают светлый, тёмный и системный режим.</p></div><ThemeControls themeStyle={learner.themeStyle} colorMode={learner.colorMode} onThemeStyle={onThemeStyle} onColorMode={onColorMode}/></section><section className="settings-card"><div><p className="eyebrow">LOCAL PROTOTYPE</p><h2>Учебные данные</h2><p>На этой стадии прогресс хранится локально в браузере. Серверная синхронизация будет отдельным слоем.</p></div><button className="danger-button" onClick={onReset}>Сбросить демо-прогресс</button></section></div>}
